{
  "name": "static-server",
  "version": "1.0.0",
  "description": "a simple static server that serves static content from the public folder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "static",
    "server"
  ],
  "author": "Marcos Maia",
  "license": "WTFPL",
  "dependencies": {
    "express": "^4.14.0",
    "morgan": "^1.7.0"
  }
}
